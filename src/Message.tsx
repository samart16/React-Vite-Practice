//PascalCasing cap first letter of every word
function Message() {
    //JSX - JavaScript XML
    const name = '';
    if (name) 
        return <h1>Hello {name}</h1>; // can call function where 'name' is; 
    return <h1>Hello World</h1>
}

export default Message;